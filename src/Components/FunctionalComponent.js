//import the React package
import React from 'react';

//create a JavaScript function (usually a function declaration or arrow function)
const FunctionalComponent = () => {
    //all components are required to return a display
    return (
        <section>
            I'm a functional component
        </section>
    )
}

//export makes component available to other parts of the app
export default FunctionalComponent;